---
"@samirdamle/nano-charts": minor
"@samirdamle/nano-charts-react": minor
---

Add `gridColor` and `gridOpacity` options to `radar()` for styling the decorative spider grid (spokes + rings). `gridColor` defaults to the chart color; `gridOpacity` defaults to 0.15 and clamps into [0, 1].
