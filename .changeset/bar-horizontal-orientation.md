---
'@samirdamle/nano-charts': patch
---

Add a `horizontal` option to `bar()` for rendering bars as horizontal rows instead of vertical columns. Value and category scales swap axes; `gap`, `radius`, negative values, and stacked segments all work the same as in vertical mode.
