{
  "name": "nano-charts-monorepo",
  "private": true,
  "license": "MIT",
  "packageManager": "pnpm@12.6.0+sha512.3ef68f951cb111ac204b4a5a16f0b2ddf0da56a96e0413e81d855d9f0b55ef926714709028e1cd00c405c2c5fb7b9e8ec4dc46777c805d0373c2f2ff00fd20ec",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "pnpm -r run build",
    "test": "pnpm -r run test",
    "typecheck": "pnpm -r run typecheck",
    "lint": "pnpm -r run lint",
    "format": "prettier --write .",
    "size": "pnpm -r run size",
    "build:demo": "node scripts/build-demo.mjs",
    "dev:demo": "node scripts/build-demo.mjs && vite demo",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "release": "pnpm run lint && pnpm run typecheck && pnpm run test && pnpm run build && pnpm run size && pnpm -r run publint && changeset publish"
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.7",
    "prettier": "^3.3.3",
    "vite": "^6.0.0"
  }
}
