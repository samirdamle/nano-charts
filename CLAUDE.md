# Directives

## Playwright screenshots

- Playwright MCP screenshots and page captures go in `.playwright-mcp/` (repo root). This folder is gitignored — never commit its contents.
- Once a visual review is done and its PR has been created, delete every file in `.playwright-mcp/` before finishing the task. Don't leave stale screenshots lying around between sessions.
